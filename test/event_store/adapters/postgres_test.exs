defmodule Headwater.EventStore.Adapters.PostgresTest do
  use ExUnit.Case
  alias Headwater.EventStore.Adapters.Postgres

  describe "streams recorded events" do
    test "for an aggregate"

    test "for all recorded events"
  end
end
